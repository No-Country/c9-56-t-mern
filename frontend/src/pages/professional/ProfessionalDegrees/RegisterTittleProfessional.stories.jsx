import React from "react"

import RegisterTitleProfessional from "."

export default {
  title: "professional/RegisterTitleProfessional",
  component: RegisterTitleProfessional,
  args: {},
}

const Template = (args) => <RegisterTitleProfessional {...args} />

export const Default = Template.bind({})
Default.args = {}
