import AddBtn from "./AddBtn"

export default {
  title: "Components/AddBtn",
  component: AddBtn,
  args: {},
}

const Template = (args) => <AddBtn {...args} />

export const Default = Template.bind({})
Default.args = {}
