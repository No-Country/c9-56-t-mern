import FormDataEdit from "./FormDataEdit"

export default {
  title: "Components/FormDataEdit",
  component: FormDataEdit,
  args: {},
}

const Template = (args) => <FormDataEdit {...args} />

export const Default = Template.bind({})
Default.args = {}
