import React from "react"

import FormDataExtra from "./FormDataExtra"

export default {
  title: "RegisterProfessional/FormDataExtra",
  component: FormDataExtra,
  args: {},
}

const Template = (args) => <FormDataExtra {...args} />

export const Default = Template.bind({})
Default.args = {}
